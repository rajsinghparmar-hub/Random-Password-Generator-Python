# Random Password Generator (Python)

This is a simple Python program that generates a secure random password using letters, numbers, and special characters.

##  Features
- Generates a **12-character** random password
- Uses:
  - Uppercase & lowercase letters
  - Digits (0–9)
  - Special characters
- Every run creates a new password
